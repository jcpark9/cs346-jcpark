/*
 * sm_test.1: tests load and print
 */

/* create some relations */
create table soaps(soapid  i, sname  c28, network  c4, rating  f);
create table saps(id i, sn c28, net c4, rat f);
create table stars(starid  i, stname  c20, plays  c12, soapid  i);

print rel;

/* load tuples from ./tests/stars.data */
load soaps("../soaps.data");
load saps("../soaps.data");
load stars("../stars.data");

create index soaps(rating);

/* update soaps */
select sname, network, rat, plays from soaps, saps, stars where rat = rating and network = net and network = "ABC" and soaps.soapid = stars.soapid and rating > 5.0;

/* print out contents of soaps */
print soaps;

!ls;

exit;
